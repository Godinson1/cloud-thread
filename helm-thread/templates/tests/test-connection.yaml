apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-thread.fullname" . }}-test-connection"
  labels:
    {{- include "helm-thread.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-thread.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
