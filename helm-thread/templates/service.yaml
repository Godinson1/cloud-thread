apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-thread.fullname" . }}
  labels:
    {{- include "helm-thread.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-thread.selectorLabels" . | nindent 4 }}
